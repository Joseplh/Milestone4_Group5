Goal: 
	Implement semantics function of milestone 2 in SML.
	Implement type-checking functions from milestone 3 in SML.

Examples:
(*Representation of parse trees used by the TL system*)
datatype NODE_INFO = ...; (*ignored for now*)
datatype INODE     = inode of string * NODE_INFO;
datatype ITREE     = itree of INODE * ITREE list;

itree(rootNode, children)
itree(inode("root symbol",_),[child,child,...,child])


stmtList ::= stmt;stmtList
[[stmt1;stmtList1]]
itree(inode("stmtList",_)
	[
	  stmt1
	  itree(inode(";",_),[]),
	  stmtList1
	]
)

time 6:40, caution in id expression, do not let it be generic
assign ::= id = expression
[[id1 = expression1]]
itree(inode("assign",_),
	[
	  id1,
	  itree(inode("=",_),[]),
	  expression1
	]
)















